# 计划与心流行动验证记录

## 范围

- 日期：2026-09-07。
- 工作区：`E:\rumo_daiban`，`main`，基线 `15d3766ab83b53b203f6c90dffdc5bd5b87820b9`。验证对象为当前未提交工作区，包括此前已有的设置、主题和标签改动。
- 没有提交、推送、打标签、发布、安装，也没有读取或修改个人用户数据库。
- Node 使用 `D:\nvm\v24.16.0`，`COREPACK_ENABLE_AUTO_PIN=0`。没有新增依赖。

## 实现结果

任务计划贯穿共享契约、SQLite、草稿、IPC、界面和备份。总计划、今天、本周、本月、逾期视图使用独立于截止日期的执行计划；本周支持把周任务拖放到具体日期，本月按计划精度分区。今日重点、到期提醒及过往未完成提示分别展示，未完成不自动滚动。

批量操作集中在任务命令服务，包含完成、清单、标签、计划、截止日期和删除。删除批次区分此前单独删除的子任务；恢复与永久删除有独立语义，30 天清理保护较新删除的后代。历史搜索查询正式数据并通过草稿落盘后的导航打开任务或心流来源。

心流三问和六问共用原有草稿。正文保存、复盘完成、任务创建和任务完成互不替代。来源版本校验、草稿检查及幂等请求在主进程事务中执行；来源删除保留任务和最小来源信息。偏好组件不再依赖整个任务工作区，行动展示仅取关联任务摘要。

## 实际执行

| 检查 | 结果 | 证明范围 |
| --- | --- | --- |
| `pnpm typecheck` | 通过 | Vue 与 Electron TypeScript 契约 |
| `pnpm test` | 18 个文件、105 项通过 | 原有业务、日期规则、批量事务、来源行动、升级和备份 |
| `RUMO_E2E_PORT=5186 pnpm test:e2e` | 20 项通过 | Edge 浏览器，模拟 `todoApi`，布局、键盘、主题、表单与既有交互 |
| `pnpm build` | 通过 | 主进程、preload、renderer 当前源码构建 |
| `pnpm exec playwright test --config tests/electron.config.ts --trace retain-on-failure` | 4 项通过 | 真实 Electron、IPC、临时 SQLite、独立用户目录、多窗口和重启 |
| `git diff --check` | 通过 | 补丁空白检查；Git 的 LF/CRLF 提示不是失败 |

新增 SQLite 验证覆盖：规范化计划、跨月周与本地日期、重复实例未安排、批量回滚、无效参数、同批次恢复、乱序选择恢复、失效清单归入收集箱、30 天清理、来源版本冲突、来源草稿阻止行动、事务失败不留孤立任务、重复请求不重复创建、来源删除及任务永久删除墓碑、搜索通配符转义、选定日任务事实、v5 往返、旧备份拒绝、v8 快照完整性、迁移事务回滚，以及固定时钟下计划更新与草稿冲突检查。

真实桌面新增场景覆盖：详情计划与当日重点保存、今日重点分区、三问六问共享正文、保存前禁止创建行动、多行动、任务与来源双向跳转、历史搜索、批量删除、回收站恢复、退出重启后的计划和关联保留。

## 浏览器人工检查

使用 Playwright MCP 专用浏览器，新开 `http://127.0.0.1:5187/`，注入本仓库浏览器测试的模拟接口。未操作既有 `127.0.0.1:5174` 标签页。

- 检查本月分区、心流三问与展开六问、设置换肤和最小窗口布局。
- 1440×900 浅色、1024×720 深色；最小窗口 `body.scrollWidth === innerWidth === 1024`。
- 确认三问和六问可见字段数分别为 3 和 6；控制台 0 error、0 warning。
- 浏览器没有业务 HTTP 请求，因为桌面接口由测试模拟；它不证明真实 SQLite 持久化。真实链路由上表 Electron 测试单独验证。
- 本地截图位于忽略目录 `test-results/`：`planning-month-light.png`、`planning-flow-three-questions.png`、`planning-flow-dark-minimum.png`、`planning-flow-three-questions-final-dark.png`、`planning-desktop-month.png`。验证服务器冷启动复核三问编号为 1、2、3；静态网络记录未发现失败请求。
- 验证完成后关闭本次新建的浏览器标签页及 5187 验证服务器，保留原有 5174 标签页和其他应用进程。

## 验证边界与异常记录

中途曾发生一条既有 Electron 备份/退出测试超时；单项带 trace 重跑通过，最终完整 4 项桌面回归通过。未将这次非稳定复现解释为已确定并修复的业务缺陷。测试中故意触发的主题保存错误用于验证回滚；Node/Playwright 的环境色彩和子进程弃用提示不影响最终结果。

没有执行 NSIS 打包、安装升级、个人数据库迁移、远程发布或旧版本现场回退。迁移只在临时测试数据库验证：升级前一致性快照、旧任务未安排、草稿升级至 v2、失败回滚。生产使用前仍应核对安装包与源版本；本次源码修改不会更新已有 Release。

备份格式已切换为 v5-only，v1–v4 明确拒绝。需要降级时必须关闭全部应用进程，并使用升级前 SQLite 快照，不能让旧程序打开升级后的数据库。
